spec_version: 2
description: Shell test

  
grains:
  setup:
    kind: shell
    spec: 
      agent:
        name: dbs-mac
      activities:
        deploy:
          commands:
            - "echo setup script is running..."
        destroy:
          commands:
            - "echo destroy activity..."
